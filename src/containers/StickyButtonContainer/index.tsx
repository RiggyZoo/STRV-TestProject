import React, { FC } from 'react'
import { Container } from './styles'

const StickyButtonContainer: FC = ({ children }) => {
  return <Container>{children}</Container>
}

export default StickyButtonContainer
